:{$PORT} {
    root * /var/www/public
    encode gzip
    
    # Forward request PHP ke PHP-FPM yang jalan di port 9000
    php_fastcgi 127.0.0.1:9000
    
    # Sajikan file statis (js, css, images)
    file_server
}